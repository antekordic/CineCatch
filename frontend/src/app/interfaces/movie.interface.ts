export interface Movie {
  id: number;
  title: string;
  
  trailer: string;

  watched: boolean;
  saved: boolean;
}

/* year: number;
  genre: string;
  director: string;
  plot: string;
  rating: number;
  image: string;
  */