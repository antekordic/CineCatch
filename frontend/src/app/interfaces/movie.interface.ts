export interface Movie {
  id: number;
  title: string;
  year: number;
  genre: string,
  director: string,
  plot: string,
  rating: number,
  imageUrl: string,
  trailerUrl: string;
  watched: boolean;
  saved: boolean;
}

/* year: number;
  genre: string;
  director: string;
  plot: string;
  rating: number;
  image: string;
  */